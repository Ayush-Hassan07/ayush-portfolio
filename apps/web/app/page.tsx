import HomeHero from "../components/home/Hero/HomeHero";
import { getPublicProfile, getPublicProjects, getPublicPublications, getPublicSkills } from "../lib/public-api";
import styles from "./page.module.css";
export default async function HomePage(){const [profile,projects,publications,skills]=await Promise.all([getPublicProfile(),getPublicProjects(),getPublicPublications(),getPublicSkills()]);const technologies=new Set(projects.flatMap(p=>p.project_technology?.map(({technology})=>technology.id)??[]));return <main className={styles.page}><HomeHero name={profile?.name??"Ayush Hassan Raiyan"} title={profile?.title} bio={profile?.bio} location={profile?.location} profileImage={profile?.profile_image} resumeUrl={profile?.resume_url} projects={projects.length} publications={publications.length} technologies={technologies.size} capabilities={skills.length}/></main>}
