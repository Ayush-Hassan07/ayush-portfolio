import Link from "next/link"; import {getPublicCertifications} from "../../lib/public-api";
export default async function Certifications(){const items=await getPublicCertifications();return <main className="publication-page shell"><Link className="text-link" href="/">← Back home</Link><p className="eyebrow detail-eyebrow">Proof / Credentials</p><h1>Licenses &<br/><em>certifications.</em></h1><div className="publication-list">{items.map((x)=> <article className="publication" key={x.id}>{x.image_url&&<img className="certificate-thumb" src={`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000"}${x.image_url}`} alt=""/>}<div><p className="publication-meta">{x.issuer} {x.issue_date?`· ${x.issue_date.slice(0,4)}`:""}</p><h2>{x.name}</h2><p>{x.description}</p>{x.credential_url&&<a className="text-link" href={x.credential_url} target="_blank" rel="noreferrer">Verify credential ↗</a>}</div></article>)}</div></main>}
