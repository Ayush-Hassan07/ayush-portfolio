import Link from "next/link";
import { getPublicPublications } from "../../lib/public-api";

export default async function PublicationsPage() { const items = await getPublicPublications(); return <main className="publication-page shell"><Link className="text-link" href="/">← Return home</Link><p className="eyebrow">Research / Archive</p><h1>Questions worth<br /><em>following.</em></h1><div className="publication-list">{items.length ? items.map((item, index) => <article className="publication" key={item.id}><span className="publication-no">0{index + 1}</span><div><p className="publication-meta">{item.venue ?? "RESEARCH RECORD"}</p><h2>{item.title}</h2><p>{item.description ?? "Research record and contribution details will be added from the admin workspace."}</p>{item.paper_url && <a className="text-link" href={item.paper_url} target="_blank" rel="noreferrer">Read paper ↗</a>}</div></article>) : <p className="empty-editor">No published research records yet.</p>}</div></main>; }
