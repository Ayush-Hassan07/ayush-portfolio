"use client";
import Link from "next/link";
import { useState } from "react";
import styles from "./SystemsInspector.module.css";
type ProjectRecord={id:string|number;slug:string;title:string;description?:string|null;status?:string|null;number:string;technologies:string[]};
export default function SystemsInspector({projects}:{projects:ProjectRecord[]}){const [activeIndex,setActiveIndex]=useState(0);const active=projects[activeIndex];return <section className={styles.systems}><div className={styles.consoleHeader}><div><span>// SYSTEM INDEX</span><small>{String(projects.length).padStart(2,"0")} PROJECT RECORDS</small></div><div className={styles.consoleState}><i/>ARCHIVE ONLINE</div></div><div className={styles.workspace}><div className={styles.index}>{projects.map((p,i)=><button key={p.slug} type="button" className={`${styles.systemRow} ${i===activeIndex?styles.systemRowActive:""}`} onMouseEnter={()=>setActiveIndex(i)} onFocus={()=>setActiveIndex(i)} onClick={()=>{setActiveIndex(i);window.location.href=`/projects/${p.slug}`}}><span className={styles.number}>{p.number}</span><span className={styles.signal}><i/></span><span className={styles.identity}><small>{p.status??"PROJECT"}</small><strong>{p.title}</strong><span className={styles.mobileStack}>{p.technologies.slice(0,3).join(" / ")}{p.technologies.length>3?` / +${p.technologies.length-3}`:""}</span></span><span className={styles.stack}>{p.technologies.slice(0,3).map(t=><span key={t}>{t}</span>)}{p.technologies.length>3&&<span>+{p.technologies.length-3}</span>}</span><span className={styles.rowState}>{i===activeIndex?"ACTIVE":"READY"}</span></button>)}</div><aside className={styles.inspector}><div className={styles.inspectorGrid}/><header className={styles.inspectorHeader}><div><span>// PROJECT SIGNAL</span><small>RECORD {active.number} / {String(projects.length).padStart(2,"0")}</small></div><span className={styles.status}><i/>{active.status??"PROJECT"}</span></header><div className={styles.visual}><span className={styles.ringOuter}/><span className={styles.ringInner}/><div className={styles.core}><small>SYS</small><strong>{active.number}</strong></div><span className={styles.coordinate}>STACK / RECORD / ACTIVE</span></div><div className={styles.inspectorBody}><div className={styles.recordMeta}><span><small>RECORD</small><strong>{active.number}</strong></span><span><small>STATE</small><strong>{active.status??"PROJECT"}</strong></span><span><small>TECH</small><strong>{String(active.technologies.length).padStart(2,"0")}</strong></span></div><h2>{active.title}</h2>{active.description&&<p>{active.description}</p>}<div className={styles.technologyPanel}><span className={styles.techLabel}>TECHNOLOGY SIGNAL</span><div>{active.technologies.map((t,i)=><span className={styles.tech} key={`${t}-${i}`}><i/>{t}</span>)}</div></div><Link href={`/projects/${active.slug}`} className={styles.inspectAction}><span><small>OPEN RECORD</small>INSPECT SYSTEM</span><strong>↗</strong></Link></div></aside></div></section>}
