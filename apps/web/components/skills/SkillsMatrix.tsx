import SkillIcon from "./SkillIcon";import styles from "./SkillsMatrix.module.css";type Skill={id:string;name:string;category:string;sort_order:number;featured:boolean};export default function SkillsMatrix({skills}:{skills:Skill[]}){const groups=Array.from(new Set(skills.map(s=>s.category||"Capabilities")));return <section className={styles.capabilities}><header className={styles.header}><div><span>// CAPABILITY INDEX</span><small>{String(skills.length).padStart(2,"0")} RECORDED CAPABILITIES</small></div><p>Languages, frameworks, platforms and technical capabilities used across software engineering, intelligent systems and research.</p></header><section className={styles.core}><div className={styles.coreHeader}><span>CORE STACK</span><i/></div><div className={styles.coreSkills}>{skills.filter(s=>s.featured).map(s=><div className={styles.coreSkill} key={s.id}><div className={styles.coreIcon}><SkillIcon name={s.name} category={s.category} size={29}/></div><strong>{s.name}</strong></div>)}</div></section><section className={styles.groupGrid}>{groups.map((group,index)=><article className={styles.group} key={group}><header className={styles.groupHeader}><div className={styles.groupSymbol}><SkillIcon name="" category={group} size={19}/></div><div><span>{String(index+1).padStart(2,"0")}</span><h2>{group}</h2></div><small>{String(skills.filter(s=>(s.category||"Capabilities")===group).length).padStart(2,"0")}</small></header><div className={styles.skills}>{skills.filter(s=>(s.category||"Capabilities")===group).map(s=><div className={styles.skill} key={s.id}><span className={styles.skillIcon}><SkillIcon name={s.name} category={s.category} size={16}/></span><strong>{s.name}</strong></div>)}</div></article>)}</section></section>}
