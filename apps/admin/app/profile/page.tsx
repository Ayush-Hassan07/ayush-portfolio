"use client";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
const api = process.env.NEXT_PUBLIC_ADMIN_API_URL ?? "http://localhost:4000";
type Profile = { name: string; title: string; bio: string; email: string; location: string | null; github_url: string | null; linkedin_url: string | null };
const empty: Profile = { name: "", title: "", bio: "", email: "", location: "", github_url: "", linkedin_url: "" };
export default function ProfilePage() { const [form, setForm] = useState<Profile>(empty); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); useEffect(() => { void fetch(`${api}/admin/library/profile`, { credentials: "include" }).then((r) => r.ok ? r.json() : null).then((value: Profile | null) => { if (value) setForm(value); }).catch(() => setMessage("Could not load profile.")); }, []); async function save(event: FormEvent) { event.preventDefault(); setBusy(true); const response = await fetch(`${api}/admin/library/profile`, { method: "PUT", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); setMessage(response.ok ? "Profile saved." : "Could not save profile."); setBusy(false); } return <main className="editor-shell"><header className="editor-header"><Link className="admin-brand" href="/">AHR<span>.</span><small>studio / profile</small></Link><Link className="editor-back" href="/">← Overview</Link></header><section className="editor-intro"><p className="admin-kicker">Content / Profile</p><h1>Define the<br /><em>identity.</em></h1><p>Everything here can shape the public introduction without requiring a source-code edit.</p></section><form className="profile-form project-form" onSubmit={save}><label>Name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label>Professional title<input required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} /></label><label>Bio<textarea required rows={6} value={form.bio} onChange={(e) => setForm({ ...form, bio: e.target.value })} /></label><label>Public email<input type="email" required value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label>Location<input value={form.location ?? ""} onChange={(e) => setForm({ ...form, location: e.target.value })} /></label><label>GitHub URL<input type="url" value={form.github_url ?? ""} onChange={(e) => setForm({ ...form, github_url: e.target.value })} /></label><label>LinkedIn URL<input type="url" value={form.linkedin_url ?? ""} onChange={(e) => setForm({ ...form, linkedin_url: e.target.value })} /></label><button type="submit" disabled={busy}>{busy ? "Saving…" : "Save profile"}</button>{message && <p className="editor-message">{message}</p>}</form></main>; }
