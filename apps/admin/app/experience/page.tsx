"use client"; import Link from "next/link"; import {FormEvent,useEffect,useState} from "react";
type Row={id:string;company:string;position:string;location:string|null;start_date:string;end_date:string|null;description:string;company_url:string|null}; const api=process.env.NEXT_PUBLIC_ADMIN_API_URL??"http://localhost:4000"; const blank={company:"",position:"",location:"",start_date:"",end_date:"",description:"",company_url:""};
export default function ExperiencePage(){const [rows,setRows]=useState<Row[]>([]),[f,setF]=useState(blank),[edit,setEdit]=useState<string|null>(null),[msg,setMsg]=useState("");async function load(){const r=await fetch(`${api}/admin/library/experience`,{credentials:"include"});return r.json() as Promise<Row[]>}useEffect(()=>{void load().then(setRows)},[]);async function save(e:FormEvent){e.preventDefault();const r=await fetch(`${api}/admin/library/experience${edit?`/${edit}`:""}`,{method:edit?"PUT":"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(r.ok){setRows(await load());setF(blank);setEdit(null);setMsg("Experience saved.")}else setMsg("Could not save experience.")}return <main className="editor-shell"><header className="editor-header"><Link className="admin-brand" href="/">AHR<span>.</span><small>studio / experience</small></Link><Link className="editor-back" href="/">← Overview</Link></header><section className="editor-grid"><form className="project-form" onSubmit={save}><p className="admin-kicker">{edit?"Edit experience":"Add experience"}</p>{([['company','Company'],['position','Position'],['location','Location'],['start_date','Start date'],['end_date','End date'],['company_url','Company URL']] as const).map(([k,l])=><label key={k}>{l}<input type={k.includes('date')?'date':k.includes('url')?'url':'text'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<label>Description<textarea required value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><div className="form-actions"><button type="submit">{edit?"Update experience":"Add experience"}</button></div><p className="editor-message">{msg}</p></form><div className="project-list"><p className="admin-kicker">Experience · {rows.length}</p>{rows.map((r,i)=><article className="project-row" key={r.id}><span>0{i+1}</span><div><strong>{r.position}</strong><p>{r.company}{r.location?` · ${r.location}`:""}</p></div><button type="button" onClick={()=>{setEdit(r.id);setF({...r,start_date:r.start_date.slice(0,10),end_date:r.end_date?.slice(0,10)??"",location:r.location??"",company_url:r.company_url??""})}}>Edit</button></article>)}</div></section></main>}


