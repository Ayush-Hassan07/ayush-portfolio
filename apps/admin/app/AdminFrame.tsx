"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { useCallback, useEffect, useState } from "react";
const sections=[["Overview","/"],["Profile","/profile"],["Projects","/projects"],["Publications","/publications"],["Skills","/skills"],["Technologies","/technologies"],["Licenses & Certifications","/certifications"],["Education","/education"],["Experience","/experience"],["Media","/media"],["Security","/security"]];
export default function AdminFrame({children}:{children:React.ReactNode}){const pathname=usePathname();const[open,setOpen]=useState(false);const[checking,setChecking]=useState(true);const api=process.env.NEXT_PUBLIC_ADMIN_API_URL??"http://localhost:4000";const verify=useCallback(async()=>{if(pathname==="/login"){setChecking(false);return}try{const r=await fetch(`${api}/auth/session`,{credentials:"include",cache:"no-store"});const v=await r.json().catch(()=>({authenticated:false}));if(!r.ok||!v.authenticated){window.location.replace("/login");return}setChecking(false)}catch{window.location.replace("/login")}},[api,pathname]);useEffect(()=>{void verify();if(pathname==="/login")return;const timer=window.setInterval(()=>void verify(),30000);const onFocus=()=>void verify();window.addEventListener("focus",onFocus);return()=>{window.clearInterval(timer);window.removeEventListener("focus",onFocus)}},[verify,pathname]);async function logout(){try{await fetch(`${api}/auth/logout`,{method:"POST",credentials:"include"})}finally{window.location.replace("/login")}}if(pathname==="/login")return children;if(checking)return <main className="login-shell">Checking authentication…</main>;return <main className="admin-frame"><button className="mobile-menu" type="button" onClick={()=>setOpen(v=>!v)} aria-label="Toggle admin navigation">•••</button><aside className={open?"admin-sidebar mobile-open":"admin-sidebar"}><Link className="admin-brand" href="/" onClick={()=>setOpen(false)}>AHR<span>.</span><small>studio</small></Link><nav aria-label="Admin sections">{sections.map(([label,href],index)=><Link className={pathname===href?"active":""} href={href} key={label} onClick={()=>setOpen(false)}><span>{String(index+1).padStart(2,"0")}</span>{label}</Link>)}</nav><div className="admin-sidebar-foot"><span className="admin-status"/> Local workspace<br/><small>Content stays yours.</small><button className="admin-logout" type="button" onClick={()=>void logout()}>Log out</button></div></aside><section className="admin-main">{children}</section></main>}
